[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "example-plugin"
version = "0.1.0"
description = "fcbyk-cli plugin"
requires-python = ">=3.10"

[project.entry-points."fcbyk.plugins"]
example-plugin = "demo.main:register"
